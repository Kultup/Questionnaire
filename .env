# Flask Configuration
SECRET_KEY=your-super-secret-key-change-this-in-production
FLASK_ENV=development
FLASK_DEBUG=True
WTF_CSRF_ENABLED=False

# Database Configuration
# DATABASE_URL=sqlite:///instance/feedback_system.db  # Відносний шлях (не працює)
# For production use PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost/feedback_system
# Використовуємо абсолютний шлях для SQLite:
DATABASE_URL=sqlite:///C:/Users/dpytlyk-da.DREAMLAND/Desktop/Questionnaire-main/instance/feedback_system.db

# Admin Credentials
ADMIN_LOGIN=admin
ADMIN_PASSWORD=admin123

# Application Settings
APP_NAME=Система збору відгуків
APP_VERSION=1.0.0
DEBUG=True
HOST=0.0.0.0
PORT=5000

# Domain Configuration
# Для налаштування доменного імені замініть наступні значення:

# 1. DOMAIN - ваше доменне ім'я (без http/https)
# Приклад: DOMAIN=example.com або DOMAIN=feedback.myrestaurant.com
DOMAIN=localhost

# 2. BASE_URL - повний URL вашого сайту (з протоколом)
# Для HTTP: BASE_URL=http://your-domain.com
# Для HTTPS: BASE_URL=https://your-domain.com
BASE_URL=http://localhost:5000

# 3. SSL Configuration (для HTTPS)
# Розкоментуйте та налаштуйте для використання HTTPS:
# SSL_CERT_PATH=/path/to/your/certificate.crt
# SSL_KEY_PATH=/path/to/your/private.key

# Production Environment Settings
# Для продакшн середовища змініть наступні налаштування:
# FLASK_ENV=production
# FLASK_DEBUG=False
# DEBUG=False
# HOST=0.0.0.0  # Залишити як є для прослуховування всіх інтерфейсів

# ІНСТРУКЦІЇ ДЛЯ НАЛАШТУВАННЯ ДОМЕНУ:
# 
# 1. Замініть DOMAIN на ваше доменне ім'я
# 2. Замініть BASE_URL на повний URL з вашим доменом
# 3. Для продакшн середовища:
#    - Встановіть FLASK_ENV=production
#    - Встановіть FLASK_DEBUG=False
#    - Встановіть DEBUG=False
#    - Змініть SECRET_KEY на унікальний ключ
#    - Налаштуйте PostgreSQL в DATABASE_URL
# 4. Для HTTPS додайте шляхи до SSL сертифікатів
# 5. Перезапустіть сервер після внесення змін

# Email Configuration
# SMTP налаштування для відправки email повідомлень
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=

# Email Settings
MAIL_SUBJECT_PREFIX=[Відгук] 
MAIL_ENABLED=True

# Інструкції для налаштування email:
# 1. Замініть MAIL_USERNAME на вашу email адресу
# 2. Замініть MAIL_PASSWORD на пароль додатку (App Password) для Gmail
# 3. Для інших провайдерів змініть MAIL_SERVER та MAIL_PORT:
#    - Gmail: smtp.gmail.com:587
#    - Outlook: smtp-mail.outlook.com:587
#    - Yahoo: smtp.mail.yahoo.com:587
# 4. Для Gmail увімкніть двофакторну аутентифікацію та створіть пароль додатку